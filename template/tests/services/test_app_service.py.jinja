"""Tests for the app_service module."""

from {{ project_package }}.services.app_service import AppService


def test_app_service_get_version_returns_string() -> None:
    """Verify AppService.get_version returns a string."""
    service = AppService()
    result = service.get_version()
    assert isinstance(result, str)
    assert len(result) > 0
